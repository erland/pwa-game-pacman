@erland:registry=https://npm.pkg.github.com
    always-auth=true
    # paste a classic PAT with write:packages if needed, or rely on your user-level ~/.npmrc
    # //npm.pkg.github.com/:_authToken=${NPM_TOKEN}
